# 모든 텍스트 파일은 자동 감지 (기본적으로 LF 유지)
* text=auto

# C++ 관련 파일은 무조건 LF 줄바꿈 사용
*.cpp  text eol=lf
*.h    text eol=lf
*.hpp  text eol=lf

# JSON, Python 등 기타 텍스트 파일도 LF로 고정
*.json text eol=lf
*.py   text eol=lf
*.txt  text eol=lf
*.md   text eol=lf

# 바이너리 파일은 줄바꿈 감지 안 함
*.png  binary
*.jpg  binary
*.jpeg binary
*.exe  binary
*.dll  binary